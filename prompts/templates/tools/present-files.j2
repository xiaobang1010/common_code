向用户交付最终产物（打开右侧面板展示文件），是结果呈现的唯一出口。

何时调用（必须遵守）：
- 任务完成且产出了用户该看的文件（报告、文档、代码、数据等）时，结束回合前的最后一步
  必须调用本工具——不调用，用户看不到你的工作成果
- 只呈现新产出的交付物：读过的参考文件、顺带改动的中间文件不要呈现
- 产出多个文件时合并为一次调用，按查看优先级排序（第一个自动聚焦打开）；
  禁止一个文件一次调用地连发
- 任务没有可看的文件产出（纯问答、纯命令执行）时不要调用
- 部分完成或预期未来才有的结果不要提前调用

调用之后：
- 仍需在回复里简要说明交付了什么、结论是什么（用户可能只读文字），但不要复述文件全文
